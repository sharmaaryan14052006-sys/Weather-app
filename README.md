# Weather-app
A simple web app that fetches real-time weather data for any city using the OpenWeatherMap API. Built with Python and Flask on the backend, with a basic HTML/CSS frontend for search input and results display.  Add your OpenWeatherMap API key in the space provided in app.py, then run the app and search a city at localhost:5000.
